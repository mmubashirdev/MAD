Mobile Application Development

Activity Life Cycle 

onCreated() - called when the system first creates the process and initialize the view and binds the data. Only the app layout and cide is loaded intothe memory 

onStart() - It execute as soon as the app become visible to the user but still user cannot interact with it

onPause() - Runs when another process takes focus e.g call or a notification

omStop() - Runs when the app is no longer visible on the screen

onDestroy - Runs before complete process kills freom the memory


android:layout_width 
android:layput_height
style etc


Phases for Mobile application development 

design, structure (prototype), development 

hybrid - wrapping project 

cross platform - make code specific for Cross devices
